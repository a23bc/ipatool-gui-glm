{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capabilities for the main window of ipatool-gui. We use the broad `shell:default` permission (which covers spawn, execute, open, stdin-write, kill) because this app needs to spawn the bundled ipatool sidecar AND, in dev mode, ipatool from PATH. The sidecar scope itself is declared in tauri.conf.json under `plugins.shell.scope`.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "shell:allow-spawn",
    "shell:allow-execute",
    "shell:allow-open",
    "shell:allow-stdin-write",
    "shell:allow-kill",
    "dialog:default",
    "fs:default",
    "process:default",
    "os:default",
    "clipboard-manager:default",
    "notification:default",
    "log:default"
  ]
}
